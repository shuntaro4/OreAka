<views:WindowEx
    x:Class="WhatsApp.WPF.Presentation.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:views="clr-namespace:WhatsApp.WPF.Presentation.Views"
    Title="{Binding Title.Value}"
    Width="500"
    Height="80"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Closed="Window_Closed"
    ContentRendered="Window_ContentRendered">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="5,5,5,5">
            <TextBox
                x:Name="AnswerText"
                VerticalContentAlignment="Center"
                FontSize="16"
                Text="{Binding Answer.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </Grid>

        <Grid Grid.Column="1" Margin="0,5,5,5">
            <Button
                x:Name="SendButton"
                Grid.Column="1"
                Command="{Binding SaveCommand}">
                <iconPacks:PackIconFontAwesome HorizontalAlignment="Center" Kind="PaperPlaneRegular" />
            </Button>
        </Grid>
    </Grid>
</views:WindowEx>
